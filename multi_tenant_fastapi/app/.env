# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=multi_tenancy

# Tenancy
ROOT_DOMAIN=localhost        # e.g., app.example.com -> set to example.com
TENANT_DEFAULT=public        # optional fallback schema

# SQLAlchemy (optional toggles)
SQLALCHEMY_ECHO=false
POOL_SIZE=5
MAX_OVERFLOW=10
